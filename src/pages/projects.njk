---
layout: layouts/article.njk
pageTitle: "Projects."
permalink: "/projects/"
---
{% for project in projects %}
<h3>
  <a href="{{ project.name | slugify }}/">{{ project.name }}</a>
</h3>
<p>{{ project.description }}</p>
<img src="{{ project.thumbnail.url | url }}" alt="{{ project.thumbnail.alt }}" class="placeholder" >
{% endfor %}